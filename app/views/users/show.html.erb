<p style="color: green"><%= notice %></p>

<div class="card mb-3">
  <div class="row g-0">
    <div class="col-md-8">
      <img src="/assets/bob.png" class="card-img-top" alt="...">
      <div class="card-body">
        <h5 class="card-title">
          <%= @user.try(:name).to_s + @user.try(:lastname).to_s %>
        </h5>
        <!-- добавить роль -->
        <p class="card-text">
          <small class="text-body-secondary">
            role
          </small>
        </p>
        <!-- добавить ссылки на посты пользователя -->
        <p class="card-text">
          Posts:
        </p>
        <%= link_to "Back to users", users_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
